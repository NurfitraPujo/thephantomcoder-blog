---
layout: default
---
<article class="post">
  <h1><%= data.title %></h1>
  <p class="published_at">Published at: <%= data.date.strftime("%b, %d %Y") %></p>
  <p>
    <%= yield %>
  </p>
</article>

